.envrc
.validate-tools
chroma.log

secrets/
talos/
*.tgz
requirements.lock

# git update-index --no-assume-unchanged ${file}
# git update-index --assume-unchanged ${file}  # ignore further changes to a file
etc/systemd/etcd-defrag.env
etc/scripts/etcd-defrag.bash.log
