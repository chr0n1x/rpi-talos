global:
  enabled: true
  # monkastare
  tlsDisable: true
  resources:
    requests:
      memory: 256Mi
      cpu: 250m
    limits:
      memory: 256Mi
      cpu: 250m

vault:
  ui:
    enabled: true
    serviceType: NodePort
    serviceNodePort: null
    externalPort: 8200

  server:
    ha:
      enabled: false
      # replicas: 3
    ingress:
      enabled: true
      ingressClassName: nginx
      annotations:
        cert-manager.io/cluster-issuer: duckdns-letsencrypt-prod
        nginx.ingress.kubernetes.io/proxy-body-size: 8m
        nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
        nginx.ingress.kubernetes.io/ssl-passthrough: "true"
      hosts:
        - host: vault.rannet.duckdns.org
      tls:
        hosts:
          - vault.rannet.duckdns.org
        secretName: vault-tls

  # resources:
  #   requests:
  #     memory: 2Gi
  #     cpu: 1000m
  #   limits:
  #     memory: 16Gi
  #     cpu: 2000m
