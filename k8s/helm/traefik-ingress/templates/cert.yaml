---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Release.Name -}}-cert-manager-ref
  namespace: {{ .Release.Namespace }}
spec:
  secretName: {{ .Release.Name }}-cert
  dnsNames:
    {{- range $cfg := .Values.dnsNames }}
    - {{ $cfg.dns }}
    {{- end }}
  issuerRef:
    kind: {{ (.Values.issuerRef).kind | default "ClusterIssuer" }}
    name: {{ (.Values.issuerRef).name | default "duckdns-letsencrypt-prod" }}
